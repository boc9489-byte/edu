# 教育问数系统技术文档

> 文档类型：技术设计文档
> 项目名称：教育问数系统
> 技术路线：NL2SQL + 元数据 RAG + LangGraph 工作流
> 参考基础：掌柜问数项目架构 + 在线教育业务数据 + 教育问数需求说明
> 输出格式：Markdown

---

## 1. 项目概述

教育问数系统是一个面向在线教育业务场景的智能数据查询系统，目标是让运营、教务、管理人员通过自然语言完成数据查询、指标统计、维度分析和趋势对比，无需手写 SQL。需求文档明确要求系统支持报名人数、收入金额、退款金额、完课率等指标查询，并支持按时间、课程、班次、校区、学员类型等维度进行统计分析。

系统整体采用 **NL2SQL + 元数据知识库 + 大模型生成 SQL + SQL 校验执行** 的技术路线。掌柜问数项目中也说明，该类系统本质上是一个 RAG 过程：先处理用户问题，召回相关元数据，再将上下文交给大模型生成 SQL，最后执行 SQL 并返回结果。

---

## 2. 业务背景

在线教育平台覆盖课程供给、招生转化、交易支付、学习过程、互动服务和经营分析等完整业务链路，核心对象包括机构、校区、教师、学员、课程系列、班次、课次、订单、支付、退款、报名关系、考勤、视频播放、作业、考试、评价、工单等。

平台主业务链路如下：

```text
浏览课程
→ 搜索课程
→ 咨询或领券
→ 加购与下单
→ 支付报名
→ 进入班次学习
→ 提交作业与考试
→ 互动评价
→ 售后退费
```

该链路决定了系统需要重点支持以下数据分析方向：

| 分析方向 | 典型问题                     |
| ---- | ------------------------ |
| 招生分析 | 本月报名人数是多少？哪个校区报名最多？      |
| 收入分析 | 最近 30 天收入如何？本周收入和上周相比如何？ |
| 退款分析 | 哪个课程系列退款率最高？             |
| 教务分析 | 最近三个月完课率变化情况如何？          |
| 转化分析 | 哪个渠道带来的报名人数最多？           |
| 学习分析 | 哪些班次出勤率较低？               |

---

## 3. 技术目标

## 3.1 核心目标

```text
自然语言问题
→ 问题理解
→ 元数据召回
→ 指标口径匹配
→ SQL 生成
→ SQL 校验
→ SQL 执行
→ 结构化结果返回
```

系统需要实现以下技术目标：

| 目标     | 说明                          |
| ------ | --------------------------- |
| 自然语言理解 | 识别用户问题中的指标、维度、时间、排序、对比意图    |
| 元数据召回  | 从表、字段、指标、字段值中召回 SQL 生成所需上下文 |
| SQL 生成 | 基于召回上下文生成 MySQL 可执行 SQL     |
| SQL 校验 | 校验 SQL 语法、表字段、危险操作和指标口径     |
| SQL 修正 | SQL 失败时基于错误信息进行修正           |
| 结果返回   | 返回结构化数据、自然语言解释、SQL 和追踪信息    |
| 可观测性   | 记录每个节点输入、输出、耗时和错误信息         |
| 可扩展性   | 支持新增指标、表、字段、业务域和分析规则        |

---

## 4. 总体技术架构

```text
┌────────────────────────────────────┐
│            前端 / 调试页面           │
│  问题输入、流式进度展示、结果展示      │
└─────────────────┬──────────────────┘
                  │ HTTP / SSE
┌─────────────────▼──────────────────┐
│              API 接入层              │
│              FastAPI                │
│  query 接口 / health 接口 / debug 接口 │
└─────────────────┬──────────────────┘
                  │
┌─────────────────▼──────────────────┐
│             Agent 编排层             │
│             LangGraph               │
│ 问题理解 / 召回 / 过滤 / SQL生成 / 校验 │
└─────────────────┬──────────────────┘
                  │
┌─────────────────▼──────────────────┐
│            元数据知识库层             │
│ MySQL Meta + Qdrant + Elasticsearch │
│ 表信息 / 字段信息 / 指标信息 / 字段值   │
└─────────────────┬──────────────────┘
                  │
┌─────────────────▼──────────────────┐
│          教育业务数据库 edu-data       │
│  课程 / 班次 / 订单 / 支付 / 退款 / 学习 │
└────────────────────────────────────┘
```

掌柜问数项目中采用 MySQL 存储结构化元数据，使用 Qdrant 构建语义向量索引，使用 Elasticsearch 构建全文索引，并在查询阶段多路召回相关表、字段和指标定义，再交给大模型生成 SQL。教育问数系统沿用该架构，并将业务数据替换为在线教育业务数据。

---

## 5. 技术栈选型

| 技术组件          | 作用                   |
| ------------- | -------------------- |
| FastAPI       | 提供后端 API、调试接口、流式响应接口 |
| LangGraph     | 编排问数 Agent 工作流       |
| MySQL         | 存储教育业务数据和元数据         |
| Qdrant        | 存储字段、指标向量，用于语义召回     |
| Elasticsearch | 存储维度字段值，用于全文检索       |
| TEI / BGE     | 提供中文 Embedding 服务    |
| LLM           | 生成 SQL、修正 SQL、解释结果   |
| SQLAlchemy    | 异步访问 MySQL           |
| Loguru        | 日志管理和链路追踪            |
| uv            | Python 项目依赖和运行管理     |

README 中的教育业务数据项目使用 `uv sync` 安装依赖、`uv run init_db.py` 初始化数据库、`uv run -m generate.main --profile full` 生成数据，并通过 `uv run -m app.main` 启动服务。

---

## 6. 数据架构设计

## 6.1 教育业务数据层

教育业务数据来自 `edu-data`，覆盖多个业务域。

| 数据域    | 主要职责                     | 示例表                                                              |
| ------ | ------------------------ | ---------------------------------------------------------------- |
| 基础维度域  | 渠道、课程分类、题型、学习目标、学历、身份、年级 | `dim_channel`、`dim_course_category`、`dim_grade`                  |
| 用户与组织域 | 机构、校区、部门、职员、教师、学员        | `org_institution`、`org_campus`、`staff_profile`、`student_profile` |
| 课程域    | 课程系列、班次、课次、教学资源、作业、考试    | `series`、`series_cohort`、`series_cohort_session`                 |
| 题库域    | 题库、题目、作业题目、考试题目          | `question_bank`、`question`                                       |
| 营销域    | 优惠券模板和适用范围               | `coupon`、`coupon_series_rel`                                     |
| 转化域    | 曝光、访问、搜索、收藏、购物车、咨询、领券    | `series_visit_log`、`consultation_record`                         |
| 交易域    | 订单、支付、退款                 | `order`、`payment_record`、`refund_request`                        |
| 履约域    | 报名关系、在读状态、完课状态           | `student_cohort_rel`                                             |
| 学习过程域  | 出勤、视频、作业、考试、讨论、评价        | 学习行为相关表                                                          |
| 经营衍生域  | 课酬、返佣、风控、审核              | 经营衍生结果表                                                          |

README 明确说明在线教育平台覆盖组织与资源、用户与档案、课程与教务、招生与营销、交易与履约、学习与互动、经营与风控等核心能力。

---

## 6.2 元数据知识库层

元数据知识库负责把数据库结构变成大模型可理解、可检索、可约束的知识。

### 6.2.1 元数据表设计

| 表名              | 职责                             |
| --------------- | ------------------------------ |
| `table_info`    | 存储业务表信息，如表名、中文名、业务描述、所属主题域     |
| `column_info`   | 存储字段信息，如字段名、字段类型、字段角色、字段释义、示例值 |
| `metric_info`   | 存储指标信息，如指标名称、业务含义、计算口径、关联字段    |
| `column_metric` | 存储字段与指标之间的依赖关系                 |

掌柜问数文档中的元数据库包含 `metric_info`、`column_metric`、`column_info`、`table_info` 四张核心表，用于支撑表结构、字段定义、指标口径和 SQL 生成。

---

### 6.2.2 字段角色定义

| 字段角色          | 含义              | 示例                                         |
| ------------- | --------------- | ------------------------------------------ |
| `primary_key` | 主键              | `id`                                       |
| `foreign_key` | 外键              | `student_id`、`cohort_id`                   |
| `dimension`   | 分析维度，用于筛选、分组、排序 | `campus_name`、`series_name`、`channel_name` |
| `measure`     | 度量字段，用于聚合计算     | `paid_amount`、`refund_amount`              |

掌柜问数文档中也将字段角色划分为主键、外键、度量和维度，其中度量主要用于统计计算，维度主要用于筛选和分组。

---

## 7. 核心指标设计

## 7.1 指标总览

| 指标   | 业务定义                       | 推荐计算方式                       |
| ---- | -------------------------- | ---------------------------- |
| 报名人数 | 指定时间范围内成功报名班次的学员数          | `COUNT(DISTINCT student_id)` |
| 收入金额 | 指定时间范围内支付成功的订单实付金额         | `SUM(paid_amount)`           |
| 退款金额 | 指定时间范围内退款成功金额              | `SUM(refund_amount)`         |
| 退款率  | 退款金额 / 支付金额，或退款订单数 / 支付订单数 | 比例计算                         |
| 完课率  | 完成学习人数 / 有效报名人数            | 比例计算                         |
| 出勤率  | 实际出勤人次 / 应出勤人次             | 比例计算                         |
| 咨询数  | 指定时间范围内咨询记录数量              | `COUNT(*)`                   |
| 转化率  | 支付报名人数 / 咨询人数或访问人数         | 比例计算                         |

---

## 7.2 报名人数指标

```yaml
metric_name: 报名人数
metric_code: enrollment_count
description: 指定时间范围内成功报名班次的去重学员数量
main_table: student_cohort_rel
time_field: enrolled_at
aggregation: COUNT(DISTINCT student_id)
default_filter:
  - enroll_status IN ('active', 'completed')
dimensions:
  - date
  - series
  - cohort
  - campus
  - learner_identity
  - channel
```

---

## 7.3 收入金额指标

```yaml
metric_name: 收入金额
metric_code: paid_amount
description: 指定时间范围内支付成功订单的实付金额
main_table: payment_record
time_field: paid_at
aggregation: SUM(paid_amount)
default_filter:
  - payment_status = 'success'
dimensions:
  - date
  - series
  - cohort
  - campus
  - channel
```

---

## 7.4 退款金额指标

```yaml
metric_name: 退款金额
metric_code: refund_amount
description: 指定时间范围内退款成功的金额
main_table: refund_request
time_field: refunded_at
aggregation: SUM(refund_amount)
default_filter:
  - refund_status = 'success'
dimensions:
  - date
  - series
  - cohort
  - campus
  - channel
```

---

## 7.5 完课率指标

```yaml
metric_name: 完课率
metric_code: completion_rate
description: 完成学习人数占有效报名人数的比例
main_table: student_cohort_rel
numerator: COUNT(DISTINCT CASE WHEN study_status = 'completed' THEN student_id END)
denominator: COUNT(DISTINCT CASE WHEN enroll_status IN ('active', 'completed') THEN student_id END)
formula: numerator / denominator
dimensions:
  - date
  - series
  - cohort
  - campus
  - learner_identity
```

---

## 8. 核心流程设计

## 8.1 离线流程：元数据知识库构建

```text
读取教育业务数据库表结构
        │
        ▼
抽取表信息、字段信息、字段类型
        │
        ▼
补充字段中文释义、字段别名、字段角色
        │
        ▼
配置指标定义、指标口径、关联字段
        │
        ▼
写入 MySQL Meta
        │
        ├── table_info
        ├── column_info
        ├── metric_info
        └── column_metric
        │
        ▼
构建 Qdrant 向量索引
        │
        ├── 字段信息向量
        └── 指标信息向量
        │
        ▼
构建 Elasticsearch 字段值索引
        │
        ├── 校区名称
        ├── 课程名称
        ├── 班次名称
        ├── 渠道名称
        └── 学员类型
```

掌柜问数文档中说明，元数据知识库会将完整元数据统一存储到 MySQL，并对部分关键信息构建向量索引和全文索引，以提升语义召回和关键词召回效果。

---

## 8.2 在线流程：自然语言问数

```text
用户输入问题
        │
        ▼
FastAPI 接收请求
        │
        ▼
初始化 QueryState
        │
        ▼
LangGraph 执行问数工作流
        │
        ├── 问题预处理
        ├── 关键词抽取
        ├── 指标召回
        ├── 字段召回
        ├── 字段值召回
        ├── 召回结果合并
        ├── 指标过滤
        ├── 表字段过滤
        ├── SQL 生成
        ├── SQL 校验
        ├── SQL 修正
        └── SQL 执行
        │
        ▼
返回结构化结果
```

掌柜问数文档中的问数智能体也包含字段信息召回、指标信息召回、字段值召回、过滤指标信息、过滤表信息、生成 SQL、验证 SQL、校正 SQL 和执行 SQL 等节点。

---

## 9. Agent 工作流设计

## 9.1 为什么使用 LangGraph

NL2SQL 不是一次简单的大模型调用，而是一个包含：

```text
理解 → 检索 → 生成 → 校验 → 安全控制 → 执行 → 修复
```

的完整工作流。掌柜问数文档中也明确指出，LangGraph 适合用状态机、图、循环、分支和可观测能力，把复杂的 NL2SQL 流程变成可控、可靠、可生产的智能体。

---

## 9.2 QueryState 设计

```python
class QueryState(TypedDict, total=False):
    request_id: str
    query: str
    normalized_query: str

    keywords: list[str]

    recalled_metrics: list[dict]
    recalled_columns: list[dict]
    recalled_values: list[dict]

    selected_metrics: list[dict]
    selected_tables: list[dict]
    selected_columns: list[dict]

    context: str

    generated_sql: str
    validated: bool
    sql_error: str
    corrected_sql: str

    result_rows: list[dict]
    answer: str

    trace: dict
```

---

## 9.3 节点设计

| 节点                   | 职责       | 输入                          | 输出                                   |
| -------------------- | -------- | --------------------------- | ------------------------------------ |
| `PreprocessNode`     | 标准化用户问题  | `query`                     | `normalized_query`                   |
| `KeywordExtractNode` | 抽取关键词    | `normalized_query`          | `keywords`                           |
| `MetricRecallNode`   | 召回候选指标   | `query`、`keywords`          | `recalled_metrics`                   |
| `ColumnRecallNode`   | 召回候选字段   | `query`、`keywords`          | `recalled_columns`                   |
| `ValueRecallNode`    | 召回字段真实取值 | `keywords`                  | `recalled_values`                    |
| `MergeContextNode`   | 合并多路召回结果 | 召回结果                        | `context`                            |
| `MetricFilterNode`   | 过滤指标     | `recalled_metrics`          | `selected_metrics`                   |
| `TableFilterNode`    | 过滤表字段    | `recalled_columns`          | `selected_tables`、`selected_columns` |
| `SQLGenerateNode`    | 生成 SQL   | `query`、`context`           | `generated_sql`                      |
| `SQLValidateNode`    | 校验 SQL   | `generated_sql`             | `validated`、`sql_error`              |
| `SQLCorrectNode`     | 修正 SQL   | `generated_sql`、`sql_error` | `corrected_sql`                      |
| `SQLExecuteNode`     | 执行 SQL   | SQL                         | `result_rows`                        |
| `AnswerBuildNode`    | 构造最终回答   | 查询结果                        | `answer`                             |

---

## 10. 多路召回设计

## 10.1 指标召回

指标召回目标是从 `metric_info` 中找到和用户问题最相关的指标。

示例：

```text
用户问题：最近三个月完课率变化情况
召回指标：
- 完课率
- 完课人数
- 有效报名人数
```

召回方式：

```text
query → embedding → Qdrant metric collection → top_k metrics
```

---

## 10.2 字段召回

字段召回目标是找到 SQL 需要使用的表字段。

示例：

```text
用户问题：哪个校区报名人数最多
召回字段：
- org_campus.campus_name
- student_cohort_rel.student_id
- student_cohort_rel.enrolled_at
- student_cohort_rel.enroll_status
```

召回方式：

```text
query → embedding → Qdrant column collection → top_k columns
```

---

## 10.3 字段值召回

字段值召回目标是匹配用户问题中的真实业务值。

示例：

```text
用户问题：北京朝阳校区本月收入是多少
召回字段值：
- value: 北京朝阳校区
- column: org_campus.campus_name
```

召回方式：

```text
keywords → Elasticsearch value index → matched values
```

掌柜问数项目中 Elasticsearch 主要用于对维度字段实际取值建立倒排索引，当用户问题包含“华北地区”“数码品类”等自然语言维度描述时，可以匹配数据库真实字段值，用于 WHERE 条件或 GROUP BY。教育项目中对应替换为校区、课程、班次、渠道、学员类型等字段值。

---

## 11. SQL 生成设计

## 11.1 Prompt 输入结构

```text
你是教育数据分析 SQL 专家。

用户问题：
{query}

候选指标：
{selected_metrics}

候选表：
{selected_tables}

候选字段：
{selected_columns}

候选字段值：
{recalled_values}

数据库类型：
MySQL

生成要求：
1. 只能生成 SELECT 查询
2. 只能使用候选表和候选字段
3. 指标必须遵循指标口径
4. 字段值必须优先使用召回到的真实值
5. 多表查询必须写明确 JOIN 条件
6. 趋势问题必须按时间粒度分组
7. 排名问题必须使用 ORDER BY
8. 只输出 SQL，不要输出解释
```

---

## 11.2 SQL 生成约束

| 约束      | 说明                                  |
| ------- | ----------------------------------- |
| 只读约束    | 只允许 `SELECT`                        |
| 表字段约束   | 只能使用召回到的表和字段                        |
| 指标口径约束  | 聚合逻辑必须来自 `metric_info`              |
| 时间约束    | 时间类问题必须生成时间过滤条件                     |
| JOIN 约束 | 多表查询必须有明确关联路径                       |
| 排名约束    | “最多”“最高”“TopN”类问题必须排序               |
| 趋势约束    | “趋势”“变化情况”类问题必须按时间分组                |
| 安全约束    | 禁止 `DELETE`、`UPDATE`、`DROP`、`ALTER` |

---

## 11.3 SQL 示例

### 问题

```text
本月各校区报名人数排名
```

### 预期 SQL

```sql
SELECT
    oc.campus_name AS campus_name,
    COUNT(DISTINCT scr.student_id) AS enrollment_count
FROM student_cohort_rel scr
JOIN series_cohort sc ON scr.cohort_id = sc.id
JOIN org_campus oc ON sc.campus_id = oc.id
WHERE scr.enrolled_at >= DATE_FORMAT(CURDATE(), '%Y-%m-01')
  AND scr.enrolled_at < DATE_ADD(DATE_FORMAT(CURDATE(), '%Y-%m-01'), INTERVAL 1 MONTH)
  AND scr.enroll_status IN ('active', 'completed')
GROUP BY oc.campus_name
ORDER BY enrollment_count DESC;
```

---

## 12. SQL 校验与修正设计

## 12.1 校验内容

| 校验项        | 说明                              |
| ---------- | ------------------------------- |
| SQL 类型校验   | 是否为 `SELECT`                    |
| 危险语句校验     | 是否包含 `DROP`、`DELETE`、`UPDATE` 等 |
| 语法校验       | SQL 是否可解析                       |
| 表存在校验      | SQL 中表是否存在                      |
| 字段存在校验     | SQL 中字段是否存在                     |
| 指标口径校验     | 是否使用正确聚合公式                      |
| JOIN 合理性校验 | 表之间是否存在合理关联路径                   |
| 时间条件校验     | 时间类问题是否包含时间过滤                   |
| 执行校验       | SQL 是否能在 edu-data 中执行           |

---

## 12.2 修正流程

```text
SQL 校验失败
        │
        ▼
收集错误信息
        │
        ▼
构造修正 Prompt
        │
        ▼
LLM 重新生成 SQL
        │
        ▼
再次校验
        │
        ├── 成功：执行 SQL
        └── 失败：返回可解释错误
```

---

## 13. API 设计

## 13.1 问数接口

### `POST /api/v1/query`

#### 请求体

```json
{
  "query": "本月报名人数是多少？",
  "session_id": "optional-session-id",
  "stream": false
}
```

#### 响应体

```json
{
  "request_id": "req_20260518_000001",
  "question": "本月报名人数是多少？",
  "sql": "SELECT COUNT(DISTINCT student_id) AS enrollment_count FROM ...",
  "result": [
    {
      "enrollment_count": 128
    }
  ],
  "answer": "本月报名人数为 128 人。",
  "trace": {
    "keywords": ["本月", "报名人数"],
    "metrics": ["报名人数"],
    "tables": ["student_cohort_rel"],
    "columns": ["student_id", "enrolled_at", "enroll_status"]
  }
}
```

---

## 13.2 流式问数接口

### `POST /api/v1/query/stream`

用于前端展示 Agent 执行进度。

#### SSE 事件示例

```text
event: progress
data: {"node": "KeywordExtractNode", "message": "关键词抽取完成"}

event: progress
data: {"node": "MetricRecallNode", "message": "指标召回完成"}

event: sql
data: {"sql": "SELECT ..."}

event: result
data: {"rows": [{"enrollment_count": 128}]}

event: done
data: {"answer": "本月报名人数为 128 人。"}
```

掌柜问数文档中提到问数智能体需要实时输出执行进度和查询结果，以保证前端有较好的用户体验。

---

## 13.3 元数据构建脚本

```bash
python -m app.scripts.build_meta_knowledge -c ./conf/meta_config.yaml
```

脚本职责：

```text
加载配置
→ 初始化 MySQL / Qdrant / ES / Embedding 客户端
→ 同步 table_info
→ 同步 column_info
→ 同步 metric_info
→ 同步 column_metric
→ 构建字段向量索引
→ 构建指标向量索引
→ 构建字段值全文索引
```

掌柜问数文档中也使用 `python -m app.scripts.build_meta_knowledge -c .\conf\meta_config.yaml` 构建元数据知识库，并在执行后检查 meta 数据库、ES 和 Qdrant 中是否写入数据。

---

## 14. 项目目录设计

```text
edu-data-agent/
├── app/
│   ├── agent/
│   │   ├── graph.py
│   │   ├── state.py
│   │   └── nodes/
│   │       ├── preprocess.py
│   │       ├── extract_keywords.py
│   │       ├── recall_metric.py
│   │       ├── recall_column.py
│   │       ├── recall_value.py
│   │       ├── merge_context.py
│   │       ├── filter_metric.py
│   │       ├── filter_table.py
│   │       ├── generate_sql.py
│   │       ├── validate_sql.py
│   │       ├── correct_sql.py
│   │       ├── execute_sql.py
│   │       └── build_answer.py
│   │
│   ├── api/
│   │   ├── query_api.py
│   │   └── health_api.py
│   │
│   ├── clients/
│   │   ├── mysql_client_manager.py
│   │   ├── qdrant_client_manager.py
│   │   ├── es_client_manager.py
│   │   └── embedding_client_manager.py
│   │
│   ├── conf/
│   │   ├── app_config.py
│   │   └── meta_config.py
│   │
│   ├── core/
│   │   ├── log.py
│   │   ├── context.py
│   │   ├── exception.py
│   │   └── lifecycle.py
│   │
│   ├── models/
│   │   ├── meta/
│   │   └── business/
│   │
│   ├── repositories/
│   │   ├── mysql/
│   │   │   ├── meta/
│   │   │   └── edu/
│   │   ├── qdrant/
│   │   └── es/
│   │
│   ├── services/
│   │   ├── query_service.py
│   │   ├── meta_build_service.py
│   │   └── sql_service.py
│   │
│   └── scripts/
│       └── build_meta_knowledge.py
│
├── conf/
│   ├── app_config.yaml
│   └── meta_config.yaml
│
├── prompts/
│   ├── extract_keywords.prompt
│   ├── filter_metric.prompt
│   ├── filter_table.prompt
│   ├── generate_sql.prompt
│   └── correct_sql.prompt
│
├── docker/
├── logs/
├── tests/
└── README.md
```

掌柜问数文档中的工程结构也采用 `agent`、`api`、`clients`、`conf`、`core`、`models`、`prompt`、`repositories`、`scripts`、`services` 等分层目录。

---

## 15. 配置设计

## 15.1 应用配置

```yaml
server:
  host: 0.0.0.0
  port: 8000

logging:
  file:
    enable: true
    level: INFO
    path: logs
    rotation: "10 MB"
    retention: "7 days"
  console:
    enable: true
    level: INFO

db_meta:
  host: localhost
  port: 3306
  user: root
  password: root
  database: edu_meta

db_edu:
  host: localhost
  port: 3306
  user: root
  password: root
  database: edu_data

qdrant:
  host: localhost
  port: 6333
  embedding_size: 1024

embedding:
  host: localhost
  port: 8081
  model: BAAI/bge-large-zh-v1.5

es:
  host: localhost
  port: 9200
  index_name: edu_data_values

llm:
  provider: deepseek
  model_name: deepseek-chat
  api_key: ${DEEPSEEK_API_KEY}
```

---

## 15.2 指标配置

```yaml
metrics:
  - id: enrollment_count
    name: 报名人数
    description: 指定时间范围内成功报名班次的去重学员数量
    aggregation: COUNT_DISTINCT
    measure_column: student_cohort_rel.student_id
    time_column: student_cohort_rel.enrolled_at
    filters:
      - student_cohort_rel.enroll_status IN ('active', 'completed')
    aliases:
      - 报名数
      - 报课人数
      - 参班人数

  - id: paid_amount
    name: 收入金额
    description: 指定时间范围内支付成功订单的实付金额
    aggregation: SUM
    measure_column: payment_record.paid_amount
    time_column: payment_record.paid_at
    filters:
      - payment_record.payment_status = 'success'
    aliases:
      - 收入
      - 营收
      - 实收金额
```

---

## 16. 日志与可观测性设计

## 16.1 日志字段

| 字段           | 说明         |
| ------------ | ---------- |
| `request_id` | 请求唯一标识     |
| `session_id` | 会话标识       |
| `node_name`  | 当前执行节点     |
| `query`      | 用户原始问题     |
| `input`      | 节点输入       |
| `output`     | 节点输出       |
| `latency_ms` | 节点耗时       |
| `sql`        | 生成或执行的 SQL |
| `error`      | 错误信息       |

---

## 16.2 Trace 示例

```json
{
  "request_id": "req_20260518_000001",
  "query": "本月报名人数是多少？",
  "steps": [
    {
      "node": "KeywordExtractNode",
      "latency_ms": 120,
      "output": ["本月", "报名人数"]
    },
    {
      "node": "MetricRecallNode",
      "latency_ms": 180,
      "output": ["报名人数"]
    },
    {
      "node": "SQLGenerateNode",
      "latency_ms": 1200,
      "output": "SELECT COUNT(DISTINCT student_id) ..."
    },
    {
      "node": "SQLExecuteNode",
      "latency_ms": 80,
      "output": [{"enrollment_count": 128}]
    }
  ]
}
```

需求文档明确要求系统具备较好的可观测性，能够追踪问题解析、请求生成和结果返回过程。

---

## 17. 异常处理设计

| 异常类型            | 处理策略               |
| --------------- | ------------------ |
| 问题为空            | 返回参数错误             |
| 无法识别指标          | 返回候选指标或提示用户补充问题    |
| 元数据召回为空         | 降级为关键词检索或返回无法分析原因  |
| SQL 生成失败        | 重试一次，失败后返回可解释错误    |
| SQL 校验失败        | 调用 SQL 修正节点        |
| SQL 执行失败        | 返回错误原因，并记录 SQL 和异常 |
| 查询结果为空          | 返回空结果解释，而不是系统异常    |
| LLM 超时          | 返回降级提示             |
| Qdrant / ES 不可用 | 降级为 MySQL 元数据关键词匹配 |

需求文档要求系统具备基础异常处理和降级能力，避免分析失败导致整体服务不可用。

---

## 18. 安全设计

## 18.1 SQL 安全

系统只允许执行只读查询。

禁止 SQL 类型：

```sql
INSERT
UPDATE
DELETE
DROP
ALTER
TRUNCATE
CREATE
REPLACE
GRANT
REVOKE
```

安全策略：

```text
SQL 生成后
→ 解析 SQL
→ 判断是否 SELECT
→ 检查危险关键字
→ 校验表字段白名单
→ 通过后执行
```

---

## 18.2 查询范围控制

| 控制项    | 说明                   |
| ------ | -------------------- |
| 表白名单   | 只允许查询配置过的业务表         |
| 字段白名单  | 只允许查询配置过的字段          |
| 指标白名单  | 指标必须来自 `metric_info` |
| 结果限制   | 默认追加 `LIMIT`，避免大结果集  |
| 超时控制   | SQL 执行设置超时时间         |
| 敏感字段过滤 | 手机号、邮箱等字段默认不进入问数字段   |

---

## 19. 部署与启动

## 19.1 教育业务数据初始化

```bash
uv sync

uv run init_db.py

uv run -m generate.main --profile full

uv run -m app.main
```

README 中给出了教育业务数据项目的启动方式，包括安装依赖、初始化数据库、生成数据和启动服务。

---

## 19.2 问数系统启动流程

```bash
# 1. 启动基础服务
docker compose up -d

# 2. 安装依赖
uv sync

# 3. 初始化 edu-data
uv run init_db.py

# 4. 生成教育业务数据
uv run -m generate.main --profile full

# 5. 构建元数据知识库
uv run -m app.scripts.build_meta_knowledge -c ./conf/meta_config.yaml

# 6. 启动问数服务
uv run -m app.main
```

---

## 20. 测试设计

## 20.1 功能测试用例

| 编号    | 用户问题            | 预期能力         |
| ----- | --------------- | ------------ |
| TC001 | 本月报名人数是多少？      | 基础指标查询       |
| TC002 | 最近 30 天收入如何？    | 时间趋势分析       |
| TC003 | 哪个校区报名人数最多？     | 维度分组 + 排名    |
| TC004 | 哪个课程系列退款率最高？    | 指标计算 + 排序    |
| TC005 | 最近三个月完课率变化情况    | 趋势分析         |
| TC006 | 本周和上周续费金额对比     | 时间窗口对比       |
| TC007 | 北京校区本月收入是多少？    | 字段值识别 + 条件过滤 |
| TC008 | Python 课程报名人数排名 | 课程字段值识别 + 排名 |

需求文档中的验收标准包括完成报名、收入、退款、完课率等基础指标查询，按课程、校区、班次、时间等维度统计分析，并完成趋势、对比、排名等请求。

---

## 20.2 技术测试用例

| 编号    | 测试项             | 预期结果            |
| ----- | --------------- | --------------- |
| TT001 | MySQL Meta 写入测试 | 表、字段、指标写入成功     |
| TT002 | Qdrant 字段向量召回测试 | 能召回相关字段         |
| TT003 | Qdrant 指标向量召回测试 | 能召回相关指标         |
| TT004 | ES 字段值召回测试      | 能匹配校区、课程、渠道等真实值 |
| TT005 | SQL 生成测试        | 生成 SQL 可执行      |
| TT006 | SQL 安全测试        | DDL / DML 被拒绝   |
| TT007 | SQL 修正测试        | 错误 SQL 能被修正     |
| TT008 | SSE 流式测试        | 前端能收到节点进度       |
| TT009 | 空结果测试           | 返回空结果解释         |
| TT010 | 异常降级测试          | 依赖不可用时返回可解释错误   |

---

## 21. 开发阶段规划

## 阶段一：教育数据理解与指标建模

目标：

```text
梳理 edu-data 数据域
→ 确定核心指标
→ 确定指标口径
→ 确定关键维度
→ 编写 meta_config.yaml
```

输出：

* 教育业务表清单
* 指标口径表
* 维度字段清单
* 字段释义文档

---

## 阶段二：元数据知识库实现

目标：

```text
Meta MySQL 建模
→ 表字段同步
→ 指标配置入库
→ Qdrant 向量索引
→ ES 字段值索引
```

输出：

* `table_info`
* `column_info`
* `metric_info`
* `column_metric`
* 字段向量集合
* 指标向量集合
* 字段值索引

---

## 阶段三：问数 Agent 实现

目标：

```text
LangGraph 状态定义
→ 节点开发
→ 多路召回
→ SQL 生成
→ SQL 校验
→ SQL 执行
```

输出：

* Agent Graph
* QueryState
* Recall Nodes
* SQL Nodes
* Answer Builder

---

## 阶段四：API 与前端调试页

目标：

```text
FastAPI query 接口
→ SSE 流式接口
→ 调试页面
→ 执行过程展示
```

输出：

* `/api/v1/query`
* `/api/v1/query/stream`
* Debug UI
* Swagger 文档

---

## 阶段五：测试与优化

目标：

```text
指标测试
→ 维度测试
→ 趋势测试
→ SQL 修正测试
→ 可观测性补强
```

输出：

* 测试用例
* 测试报告
* 问数样例集
* 常见错误修复记录

---

## 22. 验收标准

| 类型     | 标准                                   |
| ------ | ------------------------------------ |
| 基础指标   | 能查询报名人数、收入金额、退款金额、完课率                |
| 维度分析   | 能按课程、校区、班次、时间等维度统计                   |
| 趋势分析   | 能完成最近 30 天、最近三个月等趋势查询                |
| 对比分析   | 能完成本周 vs 上周、本月 vs 上月等对比              |
| 排名分析   | 能完成 TopN 查询                          |
| SQL 生成 | SQL 可执行，且字段、表、指标口径正确                 |
| SQL 安全 | 禁止执行非 SELECT SQL                     |
| 可解释性   | 返回 SQL、结果和口径说明                       |
| 可观测性   | 能追踪每个节点执行过程                          |
| 完整闭环   | 完成“用户提问 → 问题理解 → 分析执行 → 结果生成 → 返回结果” |

需求文档中的验收标准明确要求完成基础指标查询、维度统计分析、趋势/对比/排名请求，以及完整闭环演示。

---

## 23. 后续扩展方向

| 扩展方向     | 说明                       |
| -------- | ------------------------ |
| 多轮问数     | 支持“那上个月呢？”“按校区拆一下”等上下文追问 |
| 图表推荐     | 根据结果类型自动推荐柱状图、折线图、饼图     |
| 指标血缘     | 展示指标依赖的表、字段和计算公式         |
| 权限控制     | 按角色限制可查询数据范围             |
| 语义缓存     | 对高频问题缓存 SQL 和结果          |
| SQL 成本估计 | 执行前评估 SQL 扫描范围和复杂度       |
| 复杂分析模板   | 支持留存、转化漏斗、 cohort 分析     |
| 智能报表     | 从单次问数扩展到自动生成分析报告         |

---

## 24. 总结

教育问数系统的技术核心可以概括为：

```text
教育业务数据
+ 元数据知识库
+ 指标口径管理
+ 多路召回
+ 大模型 SQL 生成
+ SQL 校验修正
+ 结构化结果返回
```

它不是简单地让大模型直接生成 SQL，而是通过 **MySQL Meta + Qdrant + Elasticsearch** 构建可检索、可约束、可解释的教育业务元数据知识库，再使用 **LangGraph** 将问题理解、召回、过滤、生成、校验、修正、执行等步骤编排成稳定的工程化工作流。

面试时可以这样总结：

> 这个项目的关键不是“让大模型写 SQL”，而是“用教育业务元数据约束大模型写 SQL”。
> 通过元数据 RAG 解决表字段不准、指标口径不准、字段值不准的问题，再通过 SQL 校验和修正提升可执行性，最终实现面向教育业务的自然语言问数系统。
